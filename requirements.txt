# Trustworthy ML sandbox — runtime dependencies (CPU-only, no torch)
# >=1.41: st.container(key=...) / st-key-* CSS classes power the top toolbar,
# landing-card grid and mobile layout (see theme.py)
streamlit>=1.41,<2
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
plotly>=5.18
# printable-PDF export (theory + practice), with Arabic/Persian shaping
fpdf2>=2.7
arabic-reshaper>=3.0
python-bidi>=0.4
